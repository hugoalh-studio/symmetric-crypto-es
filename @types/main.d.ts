import decrypt = require("./decrypt.js");
import decryptMultipleLine = require("./decrypt-multiple-line.js");
import encrypt = require("./encrypt.js");
import encryptMultipleLine = require("./encrypt-multiple-line.js");
export const version: 2;
export { decrypt, decryptMultipleLine as decryptML, decryptMultipleLine as decryptMultiLine, decryptMultipleLine, encrypt, encryptMultipleLine as encryptML, encryptMultipleLine as encryptMultiLine, encryptMultipleLine, version as v, version as ver };
